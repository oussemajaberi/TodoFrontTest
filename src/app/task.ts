export class Task {
  id!: number;
  title!: string;
  description!: string;
  priority!: number;
  due_date!: string; // Adjust the type if necessary
  completed!: boolean;
  created_at!: string; // Adjust the type if necessary
  updated_at!: string;
}
